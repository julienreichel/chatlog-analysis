{
  "name": "chatlog-analysis-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "app",
    "amplify"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=app",
    "build": "npm run build --workspace=app",
    "test": "npm run test --workspace=app && npm run test --workspace=amplify",
    "typecheck": "npm run typecheck --workspace=app && npm run typecheck --workspace=amplify",
    "lint": "npm run lint --workspace=app && npm run lint --workspace=amplify"
  }
}
